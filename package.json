{
  "name": "fluel-frontend",
  "private": true,
  "version": "1.0.0",
  "description": "Public frontend for fluel — marketing site and Telegram Mini App",
  "scripts": {
    "install:all": "cd site && npm install && cd ../webapp && npm install",
    "dev:site": "cd site && npm run dev",
    "dev:webapp": "cd webapp && npm run dev",
    "build": "npm run build:site && npm run build:webapp",
    "build:site": "cd site && npm run build",
    "build:webapp": "cd webapp && npm run build",
    "typecheck": "cd site && npx tsc --noEmit && cd ../webapp && npx tsc --noEmit",
    "deploy": "wrangler deploy",
    "preview": "wrangler dev"
  },
  "license": "MIT",
  "devDependencies": {
    "wrangler": "^4.81.1"
  }
}
